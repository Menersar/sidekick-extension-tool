{
  "name": "scratch-extension-tool",
  "version": "0.1.0",
  "author": "Sidekick Team",
  "scripts": {
    "build": "rimraf ./build && mkdirp build && rimraf ./dist && mkdirp dist && webpack",
    "build:dist": "NODE_ENV=production yarn run build",
    "roni": "node ./roni.js",
    "build:all": "node ./buildAllExtensions.js",
    "build:extension1": "rimraf ./extensions/extension1/build && mkdirp extensions/extension1/build && rimraf ./extensions/extension1/dist && mkdirp extensions/extension1/dist && webpack extension1",
    "build:extension2": "rimraf ./extensions/extension2/build && mkdirp extensions/extension2/build && rimraf ./extensions/extension2/dist && mkdirp extensions/extension2/dist && webpack"
  },
  "dependencies": {
    "npm-add-script": "^1.1.0",
    "scratch-extension": "../sidekick-modules/scratch-extension"
  },
  "devDependencies": {
    "copy-webpack-plugin": "^10.2.0",
    "mkdirp": "^1.0.4",
    "rimraf": "^3.0.2",
    "webpack": "^5.65.0",
    "webpack-cli": "^4.9.1",
    "zip-webpack-plugin": "^4.0.1"
  }
}
